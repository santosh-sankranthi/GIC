name = "feelc-edge-worker"
main = "src/worker.ts"
compatibility_date = "2024-09-23"

# Cloudflare bundles `.wasm` imports as WebAssembly modules natively, so no extra rules are needed
# for `import wasm from "feelc/wasm/feelc.wasm"`.
