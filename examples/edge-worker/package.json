{
  "name": "@feelc-examples/edge-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker embedding feelc via the wasmBinary override (no node:fs).",
  "dependencies": {
    "feelc": "*"
  },
  "comment": "Run with: npx wrangler dev  (wrangler is intentionally not a dependency — bring your own)."
}
