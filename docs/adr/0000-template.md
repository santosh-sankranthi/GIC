# ADR 0000 — <short imperative title>

- **Status**: Proposed <!-- Proposed | Accepted | Superseded by ADR NNNN | Deprecated --> (YYYY-MM-DD)
- **Deciders**: <names>

## Context

The forces at play and the problem to solve. What constraints, prior decisions (link them), and
trade-offs frame the choice? State what made this a decision worth recording.

## Decision

What we chose, in the imperative ("Use X", "Reject Y"). Be specific enough that a future contributor
can tell whether a later change contradicts it.

## Consequences

What this buys us, what it costs, and what is deliberately left out of scope (a deferral must be
recorded here, not hidden). Note any test/golden contract the decision creates.

<!--
Optional, append-only, never rewrites the Decision above:

## Update — YYYY-MM-DD

A non-reversing follow-up (e.g. a deferred item later implemented). Also add
"amended YYYY-MM-DD (see Update)" to the Status line above.
-->
